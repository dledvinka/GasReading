<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MauiAppSample.ViewModels"
             x:Class="MauiAppSample.Views.DetailPage"
             x:DataType="viewModels:DetailPageViewModel"
             Title="{Binding Title}">
    <VerticalStackLayout>
        <Label FontSize="20"
               Margin="10"
               Text="{Binding Reading.MeterValue}" />
    </VerticalStackLayout>
</ContentPage>